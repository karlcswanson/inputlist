extends layout
block link
  -var selected = 'Network';

block append head
  script(src="/js/netscan.js")

block content
  
  table(class="table table-striped table-bordered")
    tbody#netres
  span Last network scan #{last_modified}
